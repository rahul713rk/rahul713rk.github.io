---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
  stack?: string[];
  tags?: string[];
  readingTime?: number;
}

const { title, description, stack = [], tags = [], readingTime } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<BaseLayout title={title} description={description} type="article">
  <article>
    <p class="eyebrow">Portfolio Project</p>
    <h1>{title}</h1>
    <p class="meta">{readingTime && <span>{readingTime} min read</span>}</p>
    <p class="meta">Stack: {stack.join(', ')}</p>
    <p class="tags">
      {tags.map((tag) => <a href={`${base}tags/${tag.toLowerCase()}/`}>#{tag}</a>)}
    </p>
    <slot />
  </article>
</BaseLayout>
